# Copy this file to .env and fill in real values.
# Same Supabase project the FarmSignal mobile app and this dashboard's
# legacy HTML both already use — anon key is safe to expose client-side,
# every table is RLS-gated.
VITE_SUPABASE_URL=https://hdixgdbutdepoxlplyhm.supabase.co
VITE_SUPABASE_ANON_KEY=
